/**
 * The app.
 *
 * This is a placeholder so the project boots on a fresh clone. Replace it —
 * the to-do app is built from here outward.
 *
 * See `handover/` for what to build and `archive/` for the previous
 * implementation's working parts.
 */
export function App() {
  return (
    <main>
      <h1>Anchor</h1>
      <p>Nothing built yet. Start with <code>handover/README.md</code>.</p>
    </main>
  );
}
